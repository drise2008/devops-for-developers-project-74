services:
  app:
    build:
      context: .
      dockerfile: Dockerfile        # используем обычный Dockerfile для разработки
    volumes:
      - ./app:/app
    ports:
      - "8080:8080"
    command: make dev